<h1>Sessions#new</h1>

<% if signed_in? %>
	You are signed in as: <%= link_to current_user.name, current_user %>
<% end %>

<%= form_for :session, :url => { :action => :create } do |f| %>
	<%= f.text_field :name %>
	<%= f.submit "Log in" %>

<% end %>