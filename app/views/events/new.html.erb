<h1>Events#new</h1>

<% if signed_in? %>
	You are signed in as: <%= link_to current_user.name, current_user %>
<% end %>


<%= form_for @event do |f| %>
	<%= f.text_area :description %>
	<%= f.submit "Create" %>
<% end %>